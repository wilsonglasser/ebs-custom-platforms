# Set the IP Address correctly from proxy / load balancer.
# This example work with AWS. Change the CIDR range below to
# match your environment, if different.
set_real_ip_from    10.10.0.0/16;
real_ip_header      X-Forwarded-For;
